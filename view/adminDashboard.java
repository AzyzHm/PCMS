package view;

import javax.swing.*;

public class adminDashboard extends JFrame{
    public adminDashboard() {
        setTitle("PowerHouse – Tableau de bord - Tableau de bord de l'administrateur");
        setSize(800, 600);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLocationRelativeTo(null);
        setResizable(true);
    }
}
